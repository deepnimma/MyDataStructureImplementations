package org.deepnimma.interfaces.sets;

import org.deepnimma.interfaces.GenericStructure;

/**
 * An unordered collection that contains no duplicate elements. More formally, sets contain no pair of elements e1 and e2 such that
 * e1.equals(e2), and at most one null element.
 *
 * @param <E> Any class extending {@link Object}
 */
public interface DNSet<E> extends GenericStructure<E> {
}
